import 'package:flutter/material.dart';

class AppColors {
  // Primary Red - Used for Splash, FAB, and Active Categories
  static const Color primaryRed = Color(0xFFEF2A39);

  // Secondary Red - Slightly softer for gradients or light overlays
  static const Color secondaryRed = Color(0xFFFF4B55);

  // Backgrounds
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color offWhite = Color(
    0xFFF5F5F5,
  ); // Used for Search bar/Input fields

  // Buttons & Text
  static const Color deepCharcoal = Color(
    0xFF3C2F2F,
  ); // "Order Now" & Dark text
  static const Color textGrey = Color(0xFF6A6A6A);
  static const Color categoryGrey = Color(
    0xFFF3F4F6,
  ); // Subtitles & Ingredients
  // Subtitles & Ingredients

  // Status Colors
  static const Color starYellow = Color(0xFFFFC107); // Rating stars
  static const Color successGreen = Color(0xFF4CAF50);

  // Customization Specific
  static const Color mildGreen = Color(0xFF4CAF50); // "Mild" label on slider
  static const Color hotRed = Color(0xFFFF0000); // "Hot" label on slider
}
